{
  "name": "offline-verifier",
  "version": "0.0.2",
  "description": "Detect and Handle browser connection",
  "authors": [
    "Roy Surjano <rsurjano@outlook.com> (https://codex-media.com)"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/codex-media/offline-verifier.git"
  },
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm build",
    "test-code-coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --no-colors -- test/ -R spec",
    "test": "npm run test-code-coverage && npm run lint",
    "build": "tsc -p . && npm run umd",
    "umd": "browserify src/index.js --standalone OfflineVerifier > bundler/offline-verifier.umd.js",
    "lint": "tslint src/**/*.ts -e \"**/*.d.ts\" -t verbose -o code-quality.html --force"
  },
  "dependencies": {},
  "typings": "index.d.ts",
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^1.1.0-alpha.1",
    "jsdom": "^9.8.3",
    "mocha": "^2.5.3",
    "tslint": "^3.15.1",
    "typescript": "^2.0.7"
  }
}
